# TODO

- [ ] Add challenge conditions to the headless output
- [ ] Add linter
- [ ] Add CI job. Amend AI instructions to always run CI before committing. CI does lint fix, build and run all tests.
- [ ] Logger - abstraction for app logs. Needs debug, info, warning, error. Headless routes to stdout, browser uses console.log
- [ ] Headless sandbox mode. Assign # of elevators, floors, and output more metrics. Requires Logger